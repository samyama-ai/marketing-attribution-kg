# data/

Synthetic-but-realistic journey data + generator.

Goal: journeys that look real enough to be convincing — multiple channels,
plausible path lengths, assist chains, time gaps, and a conversion rate that
makes the attribution models diverge (so the demo actually shows a difference).
